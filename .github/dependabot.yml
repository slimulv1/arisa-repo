# Keep GitHub Actions pinned to immutable commit SHAs (supply-chain safety)
# and let Dependabot open PRs when upstream tags move.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 10
    commit-message:
      prefix: ci
