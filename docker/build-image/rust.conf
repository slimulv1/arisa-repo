#!/hint/bash
# shellcheck disable=2034

#
# /etc/makepkg.conf.d/rust.conf  (baked into the Arisa build image)
#
# Source: CachyOS/docker-makepkg @ 9b0259d98e535487de07b44d7e0e7d73b7a6be29
#   https://github.com/CachyOS/docker-makepkg/blob/9b0259d98e535487de07b44d7e0e7d73b7a6be29/docker-makepkg-v3/rust.conf
# Pinned 2026-08-13 for reproducible builds. Bump deliberately.
#

#########################################################################
# RUST LANGUAGE SUPPORT
#########################################################################

# Flags used for the Rust compiler, similar in spirit to CFLAGS. Read
# linkman:rustc[1] for more details on the available flags.
RUSTFLAGS="-C opt-level=3 -C target-cpu=x86-64-v3 -Clink-arg=-z -Clink-arg=pack-relative-relocs"

# Additional compiler flags appended to `RUSTFLAGS` for use in debugging.
# Usually this would include: ``-C debuginfo=2''. Read linkman:rustc[1] for
# more details on the available flags.
DEBUG_RUSTFLAGS="-C debuginfo=2"